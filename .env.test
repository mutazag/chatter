# Test database configuration
# You can set this to a separate test database for isolation
TEST_DATABASE_URL="postgresql://username:password@localhost:5432/chatter_test"

# If you prefer to use the same database as development but with proper cleanup
# DATABASE_URL="postgresql://username:password@localhost:5432/chatter_dev"
NODE_ENV=test